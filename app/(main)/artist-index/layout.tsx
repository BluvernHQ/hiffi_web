import type { ReactNode } from "react"

/** Layout defers metadata to each route's generateMetadata. */
export default function ArtistIndexLayout({ children }: { children: ReactNode }) {
  return children
}
